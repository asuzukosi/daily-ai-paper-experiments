#!/usr/bin/env bash
# EXAMPLE ONLY — stop/terminate a paid RunPod pod manually.
set -euo pipefail
echo "=== EXAMPLE: stop RunPod pod ==="
echo "Use the RunPod console or CLI to stop/terminate your pod to avoid idle cost."
echo "This script intentionally does not call the RunPod API."
exit 0
